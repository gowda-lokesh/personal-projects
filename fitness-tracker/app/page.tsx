"use client"
import { FormEvent, useEffect, useMemo, useState } from "react"
import { createClient } from "../lib/supabase/client"
import { Plus, Check, Trash2, LogOut, Dumbbell, Activity, Footprints, TrendingUp, BarChart3 } from "lucide-react"

type Workout={id:string;name:string;description:string|null}; type Exercise={id:string;name:string;muscle_group:string|null;equipment:string|null}
type TE={exercise_id:string;sort_order:number;target_sets:number|null;target_reps_min:number|null;target_reps_max:number|null;exercise:Exercise}; type SetRow={weight:string;reps:string;rpe:string}
type Prev={date:string;sets:{weight:number|null;reps:number|null}[]}; type Cardio={id:string;session_date:string;activity_type:string;duration_seconds:number|null;distance:number|null;distance_unit:string;avg_pace:string|null;avg_heart_rate:number|null;calories:number|null}
type Act={activity_date:string;steps:number|null;distance:number|null;calories:number|null;active_minutes:number|null}; type Point={session_date:string;volume:number;top_weight:number;estimated_1rm:number}; type Tab="strength"|"cardio"|"steps"|"progress"
const supabase=createClient(); const today=()=>new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata"}).format(new Date()); const blank=():SetRow=>({weight:"",reps:"",rpe:""})
const fmt=(s:number|null)=>{if(!s)return "—";const m=Math.floor(s/60),h=Math.floor(m/60);return h?`${h}h ${m%60}m`:`${m}m`}; const fmtDate=(d:string,opts:any)=>new Date(d+"T00:00:00").toLocaleDateString("en-IN",opts)

export default function Home(){
 const [user,setUser]=useState<any>(null),[loading,setLoading]=useState(true),[tab,setTab]=useState<Tab>("strength"),[message,setMessage]=useState(""),[saving,setSaving]=useState(false)
 const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[authMode,setAuthMode]=useState<"signin"|"signup">("signin"),[authBusy,setAuthBusy]=useState(false)
 const [date,setDate]=useState(today()),[workouts,setWorkouts]=useState<Workout[]>([]),[exercises,setExercises]=useState<Exercise[]>([]),[selectedWorkout,setSelectedWorkout]=useState("")
 const [tes,setTes]=useState<TE[]>([]),[rows,setRows]=useState<Record<string,SetRow[]>>({}),[previous,setPrevious]=useState<Record<string,Prev|null>>({}),[showSetup,setShowSetup]=useState(false),[newWorkout,setNewWorkout]=useState(""),[newExercise,setNewExercise]=useState(""),[newMuscle,setNewMuscle]=useState("")
 const [cardio,setCardio]=useState<Cardio[]>([]),[cDate,setCDate]=useState(today()),[cType,setCType]=useState("Running"),[cDuration,setCDuration]=useState(""),[cDistance,setCDistance]=useState(""),[cUnit,setCUnit]=useState("km"),[cPace,setCPace]=useState(""),[cHr,setCHr]=useState(""),[cCalories,setCCalories]=useState(""),[cNotes,setCNotes]=useState("")
 const [aDate,setADate]=useState(today()),[steps,setSteps]=useState(""),[target,setTarget]=useState("10000"),[aDistance,setADistance]=useState(""),[aCalories,setACalories]=useState(""),[activeMin,setActiveMin]=useState(""),[activity,setActivity]=useState<Act[]>([])
 const [pExercise,setPExercise]=useState(""),[range,setRange]=useState<"week"|"month"|"year">("month"),[points,setPoints]=useState<Point[]>([]),[pLoading,setPLoading]=useState(false),[sessionCount,setSessionCount]=useState(0)
 const workout=useMemo(()=>workouts.find(w=>w.id===selectedWorkout),[workouts,selectedWorkout]); const volume=Object.values(rows).flat().reduce((n,r)=>n+(Number(r.weight)||0)*(Number(r.reps)||0),0)
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setUser(data.session?.user??null);setLoading(false)});const {data:l}=supabase.auth.onAuthStateChange((_e,s)=>{setUser(s?.user??null);setLoading(false)});return()=>l.subscription.unsubscribe()},[])
 useEffect(()=>{if(user)loadData()},[user]); useEffect(()=>{if(selectedWorkout)loadTemplate(selectedWorkout)},[selectedWorkout,date]); useEffect(()=>{if(user&&tab==="cardio")loadCardio()},[user,tab]); useEffect(()=>{if(user&&tab==="steps")loadActivity()},[user,tab,aDate]); useEffect(()=>{if(user&&tab==="progress")loadProgress()},[user,tab,pExercise,range])
 async function loadData(){setLoading(true);const [{data:w},{data:e},{count}]=await Promise.all([supabase.from("workout_templates").select("id,name,description").eq("archived",false).order("created_at"),supabase.from("exercises").select("id,name,muscle_group,equipment").eq("archived",false).order("name"),supabase.from("workout_sessions").select("id",{count:"exact",head:true}).eq("status","completed")]);setWorkouts(w??[]);setExercises(e??[]);setSessionCount(count??0);if(!selectedWorkout&&w?.length)setSelectedWorkout(w[0].id);if(!pExercise&&e?.length)setPExercise(e[0].id);setLoading(false)}
 async function loadTemplate(id:string){const {data}=await supabase.from("workout_template_exercises").select("exercise_id,sort_order,target_sets,target_reps_min,target_reps_max,exercise:exercises(id,name,muscle_group,equipment)").eq("workout_id",id).order("sort_order");const items=(data??[]) as unknown as TE[];setTes(items);const r:Record<string,SetRow[]>={};items.forEach(x=>r[x.exercise_id]=Array.from({length:x.target_sets??3},blank));setRows(r);await loadPrevious(items.map(x=>x.exercise_id))}
 async function loadPrevious(ids:string[]){if(!ids.length)return;const {data}=await supabase.from("workout_sessions").select("session_date,workout_session_exercises(exercise_id,exercise_sets(weight,reps))").eq("status","completed").lt("session_date",date).order("session_date",{ascending:false}).limit(100);const map:Record<string,Prev|null>={};ids.forEach(id=>map[id]=null);for(const s of(data??[])as any[])for(const se of s.workout_session_exercises??[])if(ids.includes(se.exercise_id)&&!map[se.exercise_id])map[se.exercise_id]={date:s.session_date,sets:se.exercise_sets??[]};setPrevious(map);setRows(old=>{const n={...old};ids.forEach(id=>{const p=map[id];if(p?.sets?.length)n[id]=p.sets.map((s:any)=>({weight:s.weight?.toString()??"",reps:s.reps?.toString()??"",rpe:""}))});return n})}
 async function auth(e:FormEvent){e.preventDefault();setAuthBusy(true);const r=authMode==="signin"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});if(r.error)setMessage(r.error.message);else setMessage(authMode==="signup"?"Account created. Check your email if confirmation is enabled.":"");setAuthBusy(false)}
 async function createWorkout(e:FormEvent){e.preventDefault();if(!newWorkout.trim())return;const {data,error}=await supabase.from("workout_templates").insert({user_id:user.id,name:newWorkout.trim()}).select("id,name,description").single();if(error)return setMessage(error.message);setWorkouts(x=>[...x,data]);setSelectedWorkout(data.id);setNewWorkout("")}
 async function createExercise(e:FormEvent){e.preventDefault();if(!newExercise.trim())return;const {data,error}=await supabase.from("exercises").insert({user_id:user.id,name:newExercise.trim(),muscle_group:newMuscle.trim()||null}).select("id,name,muscle_group,equipment").single();if(error)return setMessage(error.message);setExercises(x=>[...x,data].sort((a,b)=>a.name.localeCompare(b.name)));setNewExercise("");setNewMuscle("")}
 async function addExercise(id:string){if(!selectedWorkout||tes.some(x=>x.exercise_id===id))return;const {error}=await supabase.from("workout_template_exercises").insert({workout_id:selectedWorkout,exercise_id:id,sort_order:tes.length,target_sets:3,target_reps_min:8,target_reps_max:12});if(error)setMessage(error.message);else loadTemplate(selectedWorkout)}
 function updateSet(id:string,i:number,k:keyof SetRow,v:string){setRows(x=>({...x,[id]:(x[id]??[]).map((r,j)=>j===i?{...r,[k]:v}:r)}))}
 async function finishWorkout(){if(!selectedWorkout||!tes.length)return setMessage("Choose a workout with at least one exercise.");if(!Object.values(rows).some(rs=>rs.some(r=>r.weight||r.reps)))return setMessage("Enter at least one completed set before finishing.");setSaving(true);const payload={workout_id:selectedWorkout,workout_name:workout?.name,session_date:date,started_at:new Date().toISOString(),exercises:tes.map(x=>({exercise_id:x.exercise_id,exercise_name:x.exercise.name,sets:(rows[x.exercise_id]??[]).map((r,i)=>({set_number:i+1,weight:r.weight||null,reps:r.reps||null,rpe:r.rpe||null,completed:Boolean(r.weight||r.reps)}))}))};const {error}=await supabase.rpc("finish_workout",{payload});setMessage(error?error.message:"Workout saved ✓");if(!error){await loadData();await loadTemplate(selectedWorkout)}setSaving(false)}
 async function loadCardio(){const {data}=await supabase.from("cardio_sessions").select("id,session_date,activity_type,duration_seconds,distance,distance_unit,avg_pace,avg_heart_rate,calories").order("session_date",{ascending:false}).limit(20);setCardio(data??[])}
 async function saveCardio(e:FormEvent){e.preventDefault();setSaving(true);const {error}=await supabase.from("cardio_sessions").insert({user_id:user.id,session_date:cDate,activity_type:cType,duration_seconds:cDuration?Number(cDuration)*60:null,distance:cDistance?Number(cDistance):null,distance_unit:cUnit,avg_pace:cPace||null,avg_heart_rate:cHr?Number(cHr):null,calories:cCalories?Number(cCalories):null,notes:cNotes||null});setMessage(error?error.message:"Cardio session saved ✓");if(!error){setCDuration("");setCDistance("");setCPace("");setCHr("");setCCalories("");setCNotes("");await loadCardio()}setSaving(false)}
 async function loadActivity(){const {data}=await supabase.from("daily_activity").select("activity_date,steps,distance,calories,active_minutes").order("activity_date",{ascending:false}).limit(14);setActivity(data??[]);const cur=(data??[]).find(x=>x.activity_date===aDate);if(cur){setSteps(cur.steps?.toString()??"");setADistance(cur.distance?.toString()??"");setACalories(cur.calories?.toString()??"");setActiveMin(cur.active_minutes?.toString()??"")}}
 async function saveActivity(e:FormEvent){e.preventDefault();setSaving(true);const {error}=await supabase.from("daily_activity").upsert({user_id:user.id,activity_date:aDate,steps:steps?Number(steps):null,distance:aDistance?Number(aDistance):null,calories:aCalories?Number(aCalories):null,active_minutes:activeMin?Number(activeMin):null});setMessage(error?error.message:"Daily activity saved ✓");if(!error)await loadActivity();setSaving(false)}
 async function loadProgress(){if(!pExercise)return;setPLoading(true);const days=range==="week"?7:range==="month"?30:365,d=new Date();d.setDate(d.getDate()-days);const since=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata"}).format(d);const {data}=await supabase.from("exercise_progress").select("session_date,volume,top_weight,estimated_1rm").eq("exercise_id",pExercise).gte("session_date",since).order("session_date");setPoints((data??[])as Point[]);setPLoading(false)}
 if(loading)return <main className="shell loading"><div className="spinner"/>Loading your tracker…</main>
 if(!user)return <main className="authShell"><div className="authCard"><div className="logoMark"><Dumbbell size={22}/></div><h1>Fitness Tracker</h1><p className="muted">Your training, one clean log.</p><div className="authTabs"><button className={authMode==="signin"?"active":""} onClick={()=>setAuthMode("signin")}>Sign in</button><button className={authMode==="signup"?"active":""} onClick={()=>setAuthMode("signup")}>Create account</button></div><form onSubmit={auth} className="formStack"><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password (6+ characters)" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/><button className="btn primary wide" disabled={authBusy}>{authBusy?"Please wait…":authMode==="signin"?"Sign in":"Create account"}</button></form>{message&&<div className="notice error">{message}</div>}</div></main>
 const nav=(t:Tab)=>{setTab(t);setMessage("")}; const pMax=Math.max(1,...points.map(p=>Number(p.volume)||0)),latest=points[points.length-1],first=points[0],change=first&&latest?((Number(latest.volume)-Number(first.volume))/Math.max(1,Number(first.volume)))*100:0; const pct=Math.min(100,Number(steps)/Math.max(1,Number(target))*100)
 return <main className="shell"><header className="top"><div><div className="brand">Fitness Tracker</div><div className="muted">Train. Track. Progress.</div></div><div className="nav"><button className={`pill ${tab==="strength"?"activePill":""}`} onClick={()=>nav("strength")}><Dumbbell size={14}/> Strength</button><button className={`pill ${tab==="cardio"?"activePill":""}`} onClick={()=>nav("cardio")}><Activity size={14}/> Cardio</button><button className={`pill ${tab==="steps"?"activePill":""}`} onClick={()=>nav("steps")}><Footprints size={14}/> Steps</button><button className={`pill ${tab==="progress"?"activePill":""}`} onClick={()=>nav("progress")}><BarChart3 size={14}/> Progress</button><button className="iconBtn" onClick={()=>supabase.auth.signOut()}><LogOut size={17}/></button></div></header>
 {tab==="strength"&&<><section className="hero"><div><div className="eyebrow">{date===today()?"TODAY":date}</div><h1>{workout?.name??"Start your training"}</h1><p>Log each set without leaving the flow of your workout.</p></div><button className="btn primary finish" onClick={finishWorkout} disabled={saving}>{saving?"Saving…":<><Check size={17}/> Finish Workout</>}</button></section><section className="grid stats"><div className="card"><div className="muted">Exercises</div><div className="stat">{tes.length}</div><div className="muted">in session</div></div><div className="card"><div className="muted">Volume</div><div className="stat">{Math.round(volume).toLocaleString()} kg</div><div className="muted">entered</div></div><div className="card"><div className="muted">Sessions</div><div className="stat">{sessionCount}</div><div className="muted">completed</div></div><div className="card insightCard"><TrendingUp size={18}/><strong>Next-session benchmark</strong><span className="muted">Previous performance is prefilled.</span></div></section><section className="card controls"><div className="controlGroup"><label>Date</label><input type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><div className="controlGroup workoutPicker"><label>Workout</label><div className="chipRow">{workouts.map(w=><button key={w.id} className={selectedWorkout===w.id?"chip selected":"chip"} onClick={()=>setSelectedWorkout(w.id)}>{w.name}</button>)}<button className="chip add" onClick={()=>setShowSetup(!showSetup)}><Plus size={15}/> Manage</button></div></div></section>{showSetup&&<section className="setupGrid"><div className="card"><h3>New workout</h3><form onSubmit={createWorkout} className="inlineForm"><input placeholder="e.g. Upper Body" value={newWorkout} onChange={e=>setNewWorkout(e.target.value)}/><button className="btn primary"><Plus size={16}/> Add</button></form></div><div className="card"><h3>New exercise</h3><form onSubmit={createExercise} className="inlineForm"><input placeholder="Exercise name" value={newExercise} onChange={e=>setNewExercise(e.target.value)}/><input placeholder="Muscle group" value={newMuscle} onChange={e=>setNewMuscle(e.target.value)}/><button className="btn primary"><Plus size={16}/> Add</button></form><div className="exercisePool">{exercises.filter(e=>!tes.some(t=>t.exercise_id===e.id)).slice(0,16).map(e=><button key={e.id} className="poolItem" onClick={()=>addExercise(e.id)}><Plus size={14}/>{e.name}<span>{e.muscle_group??""}</span></button>)}</div></div></section>}<section className="card workoutCard"><div className="sectionHead"><h2>Log workout</h2><p className="muted">Previous performance is shown above each exercise.</p></div>{!tes.length?<div className="empty"><Dumbbell size={28}/><strong>No exercises yet</strong><span>Open Manage to add exercises.</span></div>:<div className="exerciseList">{tes.map(x=>{const p=previous[x.exercise_id],sets=rows[x.exercise_id]??[];return <div className="exercise" key={x.exercise_id}><div className="exerciseHeader"><div><h3>{x.exercise.name}</h3><span className="muted">{x.exercise.muscle_group||"Strength"}{x.target_reps_min?` · ${x.target_reps_min}–${x.target_reps_max} reps`:""}</span></div>{p&&<div className="previous"><span>Last time · {fmtDate(p.date,{day:"numeric",month:"short"})}</span><strong>{p.sets.map(s=>s.weight!=null?`${s.weight} kg${s.reps?` × ${s.reps}`:""}`:"").filter(Boolean).join(" · ")}</strong></div>}</div><div className="setTable"><div className="setHeader"><span>SET</span><span>WEIGHT</span><span>REPS</span><span>RPE</span><span/></div>{sets.map((r,i)=><div className="setRow" key={i}><span className="setNo">{i+1}</span><input inputMode="decimal" placeholder="kg" value={r.weight} onChange={e=>updateSet(x.exercise_id,i,"weight",e.target.value)}/><input inputMode="numeric" placeholder="reps" value={r.reps} onChange={e=>updateSet(x.exercise_id,i,"reps",e.target.value)}/><input inputMode="decimal" placeholder="—" value={r.rpe} onChange={e=>updateSet(x.exercise_id,i,"rpe",e.target.value)}/><button className="removeBtn" onClick={()=>setRows(q=>({...q,[x.exercise_id]:(q[x.exercise_id]??[]).filter((_,j)=>j!==i)}))}><Trash2 size={15}/></button></div>)}<button className="addSet" onClick={()=>setRows(q=>({...q,[x.exercise_id]:[...(q[x.exercise_id]??[]),blank()]}))}><Plus size={15}/> Add set</button></div></div>})}</div>}</section></>}
 {tab==="cardio"&&<section className="pageSection"><div className="pageIntro"><div><div className="eyebrow">CARDIO</div><h1>Conditioning log</h1><p className="muted">Runs, cycling, walking, rowing and more.</p></div></div><div className="twoCol"><form className="card formCard" onSubmit={saveCardio}><h2>Log session</h2><div className="formGrid"><label>Date<input type="date" value={cDate} onChange={e=>setCDate(e.target.value)}/></label><label>Activity<select value={cType} onChange={e=>setCType(e.target.value)}><option>Running</option><option>Walking</option><option>Cycling</option><option>Rowing</option><option>Elliptical</option><option>Swimming</option><option>Other</option></select></label><label>Duration (min)<input inputMode="numeric" value={cDuration} onChange={e=>setCDuration(e.target.value)} placeholder="45"/></label><label>Distance<input inputMode="decimal" value={cDistance} onChange={e=>setCDistance(e.target.value)} placeholder="5.0"/></label><label>Unit<select value={cUnit} onChange={e=>setCUnit(e.target.value)}><option value="km">km</option><option value="mi">mi</option></select></label><label>Avg pace<input value={cPace} onChange={e=>setCPace(e.target.value)} placeholder="6:00 / km"/></label><label>Avg heart rate<input inputMode="numeric" value={cHr} onChange={e=>setCHr(e.target.value)} placeholder="145"/></label><label>Calories<input inputMode="numeric" value={cCalories} onChange={e=>setCCalories(e.target.value)} placeholder="350"/></label></div><label>Notes<textarea value={cNotes} onChange={e=>setCNotes(e.target.value)} placeholder="How did it feel?"/></label><button className="btn primary wide" disabled={saving}>{saving?"Saving…":"Save cardio session"}</button></form><div className="card historyCard"><div className="sectionHead"><h2>Recent cardio</h2><span className="muted">20 sessions</span></div><div className="historyList">{cardio.map(c=><div className="historyItem" key={c.id}><div><strong>{c.activity_type}</strong><span>{fmtDate(c.session_date,{day:"numeric",month:"short",year:"numeric"})}</span></div><div className="historyMetrics"><b>{fmt(c.duration_seconds)}</b>{c.distance!=null&&<b>{c.distance} {c.distance_unit}</b>}{c.avg_heart_rate!=null&&<b>{c.avg_heart_rate} bpm</b>}</div></div>)}{!cardio.length&&<div className="empty small">No cardio sessions yet.</div>}</div></div></div></section>}
 {tab==="steps"&&<section className="pageSection"><div className="pageIntro"><div><div className="eyebrow">DAILY ACTIVITY</div><h1>Steps & movement</h1><p className="muted">Keep daily movement visible alongside training.</p></div></div><div className="twoCol"><form className="card formCard" onSubmit={saveActivity}><h2>Daily activity</h2><div className="formGrid"><label>Date<input type="date" value={aDate} onChange={e=>setADate(e.target.value)}/></label><label>Steps<input inputMode="numeric" value={steps} onChange={e=>setSteps(e.target.value)} placeholder="10000"/></label><label>Daily target<input inputMode="numeric" value={target} onChange={e=>setTarget(e.target.value)}/></label><label>Distance (km)<input inputMode="decimal" value={aDistance} onChange={e=>setADistance(e.target.value)} placeholder="7.2"/></label><label>Calories<input inputMode="numeric" value={aCalories} onChange={e=>setACalories(e.target.value)} placeholder="400"/></label><label>Active minutes<input inputMode="numeric" value={activeMin} onChange={e=>setActiveMin(e.target.value)} placeholder="60"/></label></div>{steps&&<div className="goal"><div><strong>{Math.round(pct)}%</strong><span className="muted"> of target</span></div><div className="progressBar"><span style={{width:`${pct}%`}}/></div></div>}<button className="btn primary wide" disabled={saving}>{saving?"Saving…":"Save daily activity"}</button></form><div className="card historyCard"><div className="sectionHead"><h2>Recent days</h2><span className="muted">14 days</span></div><div className="historyList">{activity.map(a=><div className="historyItem" key={a.activity_date}><div><strong>{fmtDate(a.activity_date,{weekday:"short",day:"numeric",month:"short"})}</strong><span>{a.active_minutes??0} active min</span></div><div className="historyMetrics"><b>{(a.steps??0).toLocaleString()} steps</b>{a.distance!=null&&<b>{a.distance} km</b>}</div></div>)}{!activity.length&&<div className="empty small">No activity logged yet.</div>}</div></div></div></section>}
 {tab==="progress"&&<section className="pageSection"><div className="pageIntro"><div><div className="eyebrow">PROGRESS</div><h1>Strength trends</h1><p className="muted">Track volume, strength and consistency.</p></div><div className="rangeToggle">{(["week","month","year"] as const).map(r=><button key={r} className={range===r?"selected":""} onClick={()=>setRange(r)}>{r}</button>)}</div></div><div className="card progressControls"><label>Exercise<select value={pExercise} onChange={e=>setPExercise(e.target.value)}>{exercises.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select></label><div className="progressSummary"><div><span className="muted">Sessions</span><strong>{points.length}</strong></div><div><span className="muted">Best weight</span><strong>{Math.max(0,...points.map(p=>Number(p.top_weight)||0))} kg</strong></div><div><span className="muted">Volume trend</span><strong className={change>=0?"positive":"negative"}>{points.length>1?`${change>=0?"+":""}${change.toFixed(0)}%`:"—"}</strong></div></div></div><div className="card chartCard"><div className="sectionHead"><div><h2>{exercises.find(e=>e.id===pExercise)?.name??"Exercise"}</h2><p className="muted">Session volume · estimated 1RM</p></div><BarChart3 size={20}/></div>{pLoading?<div className="empty small"><div className="spinner"/></div>:!points.length?<div className="empty small"><TrendingUp size={25}/><span>Complete a workout with this exercise to start your trend.</span></div>:<div className="chartWrap"><div className="chartBars">{points.map((p,i)=><div className="barColumn" key={`${p.session_date}-${i}`} title={`${p.session_date}: ${Math.round(p.volume)} kg`}><div className="barValue">{Math.round(p.volume).toLocaleString()}</div><div className="bar" style={{height:`${Math.max(5,Number(p.volume)/pMax*180)}px`}}/><span>{fmtDate(p.session_date,{day:"numeric",month:"short"})}</span></div>)}</div><div className="chartInsight"><strong>Latest estimated 1RM: {latest?`${Number(latest.estimated_1rm).toFixed(1)} kg`:"—"}</strong><span className="muted">Top set: {latest?`${latest.top_weight} kg`:"—"}. Progress load or reps while form stays solid.</span></div></div>}</div><div className="grid lower"><div className="card mini"><h3>Volume</h3><p className="muted">Total weight × reps. Useful for seeing workload over time.</p></div><div className="card mini"><h3>Strength</h3><p className="muted">Estimated 1RM helps distinguish strength gains from extra volume.</p></div><div className="card mini"><h3>Consistency</h3><p className="muted">You have {sessionCount} completed strength sessions.</p></div><div className="card mini"><h3>PR signal</h3><p className="muted">Highest top weight and estimated 1RM remain visible in history.</p></div></div></section>}
 {message&&<div className={`notice ${message.includes("saved")?"success":"error"}`}>{message}</div>}</main>
}
